language: php
php:
  - '7.0'

addons:
  postgresql: "9.5"

services:
  - postgresql

before_script:
  - psql -c 'create database karmashard;' -U postgres
  - composer install
  - php bin/console doctrine:schema:create --env=test

script:
  - phpunit -c app
